namespace SPDSTools
{
    /// <summary>
    /// ToolMode:
    /// Draw - режим рисования
    /// Edit - режим редактирования
    /// None - режим ожидания выбора элемента для редактирования
    /// </summary>
    public enum ToolMode
    {
        None,
        Draw,
        Edit,
        LocateDraw
    }
}
